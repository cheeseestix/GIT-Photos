# GIT Photos

This is a dependency-free, static HTML, CSS, and JavaScript project.

## Run locally on Replit

Start the **Start application** workflow. It runs:

```sh
python3 -m http.server 5000 --bind 0.0.0.0
```

The site entry point is `index.html`. Styles are in `style.css`, and JavaScript is in `script.js`.